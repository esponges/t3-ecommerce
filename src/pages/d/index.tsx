// ideally this dashboard should be in a separate url
// but for the sake of simplicity, we will keep it here

// check orders, product stocks, etc
const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
